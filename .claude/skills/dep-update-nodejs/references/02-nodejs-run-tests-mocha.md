**Execution — Run Tests (Mocha) · Node.js**

Run the full test suite:

``` console
npm test
```

Filter tests by name (grep):

``` console
npm test -- --grep "pattern"
```

Run with coverage (via [c8](https://github.com/bcoe/c8)):

``` console
npm run test:coverage
```
